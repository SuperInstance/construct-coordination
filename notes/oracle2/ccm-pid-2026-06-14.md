# Bottle: ccm-pid CLI — 2026-06-14

## Summary
Rust CLI binary for submitting conservation-meter reports from shell scripts or cron.

## Usage
```bash
ccm-pid --disk-pct 63 --services 38 --agent cron --task pulse
```

## Output
```json
{"c":1134,"gamma_ratio":"1.98","status":"accepted"}
```

## Files
- Binary: `conservation-meter/src/bin/ccm-pid.rs`
- Installed at: `/home/ubuntu/.local/bin/ccm-pid`

## Why It Matters
Before: pulse-metric.sh needed Python HTTP POST. Now: any shell can call this.
200KB static binary, no runtime deps. Fleet-grade primitive.
