[package]
name = "construct-coordination"
version = "0.1.0"
edition = "2021"

[[example]]
name = "composition_demo"
path = "examples/composition_demo.rs"

[dependencies]
circuit-breaker = { path = "../circuit-breaker" }
rate-limiter = { path = "../rate-limiter" }
health-check = { path = "../health-check" }
retry-backoff = { path = "../retry-backoff" }
load-balancer = { path = "../load-balancer" }
config-center = { path = "../config-center" }
feature-flag = { path = "../feature-flag" }
service-discovery = { path = "../service-discovery" }
